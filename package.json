{
  "name": "api-automation-factory-runner",
  "version": "1.0.0",
  "description": "Playwright TypeScript Runner for API Automation Factory",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "typescript": "^5.0.0",
    "zod": "^3.23.0"
  }
}
